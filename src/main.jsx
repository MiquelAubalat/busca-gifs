import React from "react";
import ReactDOM from "react-dom/client";
import { GifReactProject } from "./GifReactProject";
import './index.css'

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
     <GifReactProject />
  </React.StrictMode>
);
